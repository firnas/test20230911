{% extends 'base.html.twig' %}

{% block title %}Hello IndexController!{% endblock %}

{% block body %}

    <div class="col-8 text-justify">

        {% if step is defined %}
        <p>
            Complete next steps ({{ step }}/3)

        </p>
        {% endif %}
    <h1>README.MD</h1>

    {% apply markdown_to_html %}
        {{ filedata }}
    {% endapply %}

    </div>
{% endblock %}
